
export const variantStyles = {
    primary:
        'bg-gradient-to-r from-primary to-secondary text-white border-transparent hover:opacity-90',

    secondary:
        'bg-transparent border border-primary text-primary hover:bg-primary/10',

    danger:
        'bg-red-500 text-white hover:bg-red-600 border-transparent',

    ghost:
        'bg-transparent text-foreground hover:bg-white/5 border-transparent',
    none:
    ``
};

export const sizeStyles = {
    sm: 'h-9 px-4 text-sm',

    md: 'h-11 px-6 text-base',

    lg: 'h-14 px-8 text-lg',
};